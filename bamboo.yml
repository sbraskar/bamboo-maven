version: 1
plan:
  - project-key: FP
    plan-key: PLAN
    name: My First Project
    stages:
      - name: Develpoment
        jobs:
          - name: Build
            tasks:
              - script: echo " Running Script1 in Job1."
              - script: echo " Running Script2 in Job1."
          - name: Test
            tasks:
              - script: echo " Running Script1 in Job2."
              - script: echo " Running Script2 in Job2."
      - name: Production
        jobs:
          - name: Deploy to Staging
            tasks:
              - script: echo " Running Script1 in Job1."
          - name: Deploy to Production
            tasks:
              - script: echo " Running Script1 in Job2."
